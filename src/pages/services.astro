---
import Layout from "../layouts/Layout.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import FaqSection from "../components/FaqSection.astro";
import WhatWeDoCards from "../components/WhatWeDoCards.astro";

const serviceCategories = [
  {
    categoryId: "tech",
    categoryTitle: "Technology & Engineering",
    categoryDesc:
      "Full-cycle web development, native & cross-platform mobile apps, cloud infrastructure, and AI automation.",
    color: "teal",
    items: [
      {
        id: "web",
        anchorIds: ["web-development", "tech"],
        image: "/services/web-development.webp",
        imageAlt: "Web development interfaces and responsive website screens",
        icon: `<svg width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8"><path stroke-linecap="round" stroke-linejoin="round" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/></svg>`,
        color: "teal",
        title: "Web Development",
        desc: "We craft modern, performant, and beautiful web experiences that convert visitors into customers. From high-speed marketing landing pages to complex enterprise platforms.",
        features: [
          "React / Next.js / Astro",
          "Progressive Web Apps",
          "E-Commerce Solutions",
          "CMS Integration",
          "Performance Optimization",
          "SEO-First Architecture",
        ],
      },
      {
        id: "mobile",
        anchorIds: ["mobile-development"],
        image: "/services/mobile-app-development.webp",
        imageAlt: "Mobile app development interfaces and product screens",
        icon: `<svg width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8"><path stroke-linecap="round" stroke-linejoin="round" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"/></svg>`,
        color: "teal",
        title: "Mobile App Development",
        desc: "Native iOS/Android and cross-platform mobile apps built with Flutter and React Native. Seamless user experience with offline capability and real-time syncing.",
        features: [
          "React Native / Flutter",
          "iOS & Android Native",
          "Offline-First Design",
          "Push Notifications",
          "App Store Publishing",
          "Real-Time Features",
        ],
      },
      {
        id: "cloud",
        anchorIds: ["cloud-solutions", "devops"],
        image: "/services/cloud-solutions.webp",
        imageAlt: "Cloud infrastructure dashboard and deployment workflow",
        icon: `<svg width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8"><path stroke-linecap="round" stroke-linejoin="round" d="M3 15a4 4 0 004 4h9a5 5 0 10-.1-9.999 5.002 5.002 0 10-9.78 2.096A4.001 4.001 0 003 15z"/></svg>`,
        color: "teal",
        title: "Cloud Solutions & Infrastructure",
        desc: "Architecting, migrating, and managing cloud environments across AWS, GCP, and Azure. High-availability CI/CD pipelines, container orchestration, and serverless backends.",
        features: [
          "Cloud Migration",
          "Kubernetes & Docker",
          "CI/CD Pipelines",
          "Serverless Architecture",
          "Cost Optimization",
          "Multi-Cloud Security",
        ],
      },
      {
        id: "ai",
        anchorIds: ["automation", "ai-automation"],
        image: "/services/ai-automation.webp",
        imageAlt: "AI automation dashboard and intelligent workflow tools",
        icon: `<svg width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8"><path stroke-linecap="round" stroke-linejoin="round" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>`,
        color: "teal",
        title: "Business & AI Automation",
        desc: "Leverage AI models, automated LLM integrations, and custom workflow bots to eliminate repetitive operational bottlenecks and scale output effortlessly.",
        features: [
          "Machine Learning Models",
          "LLM / ChatGPT Integration",
          "RPA Automation",
          "Predictive Analytics",
          "Workflow Bots",
          "Custom AI Agents",
        ],
      },
    ],
  },
  {
    categoryId: "strategy",
    categoryTitle: "Strategy & Consulting",
    categoryDesc:
      "Roadmaps, architecture planning, and digital transformation audits to align your software with long-term business goals.",
    color: "sky",
    items: [
      {
        id: "strategy",
        anchorIds: ["consulting"],
        image: "/services/it-consulting.webp",
        imageAlt: "IT consulting strategy session and technology planning",
        icon: `<svg width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8"><path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z"/></svg>`,
        color: "sky",
        title: "IT Consulting & Digital Strategy",
        desc: "Strategic technology consulting that aligns your digital infrastructure with your business vision. We map where you are, where you need to be, and the exact steps to get there.",
        features: [
          "Digital Audits",
          "Tech Stack Review",
          "Architecture Design",
          "Vendor Selection",
          "Team Augmentation",
          "Technology Roadmaps",
        ],
      },
    ],
  },
  {
    categoryId: "brand",
    categoryTitle: "Brand & Design",
    categoryDesc:
      "Distinctive brand identities, visual design systems, and intuitive user interfaces built for scale.",
    color: "violet",
    items: [
      {
        id: "brand",
        anchorIds: ["logo", "branding"],
        image: "/services/branding-logo-design.webp",
        imageAlt: "Brand identity showcase and custom logo design",
        icon: `<svg width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8"><path stroke-linecap="round" stroke-linejoin="round" d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01"/></svg>`,
        color: "violet",
        title: "Brand Identity & Logo Design",
        desc: "Custom logo design, full brand identity guidelines, and visual collateral. We craft memorable visual identities that stand out in crowded markets.",
        features: [
          "Custom Logo Design",
          "Brand Guidelines",
          "Typography & Color Systems",
          "Visual Assets",
          "Collateral Design",
          "Brand Re-positioning",
        ],
      },
      {
        id: "uiux",
        anchorIds: ["product-design"],
        image: "/services/uiux-product-design.webp",
        imageAlt: "UI UX interface design and user experience prototyping",
        icon: `<svg width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8"><path stroke-linecap="round" stroke-linejoin="round" d="M4 5a1 1 0 011-1h14a1 1 0 011 1v2a1 1 0 01-1 1H5a1 1 0 01-1-1V5zM4 13a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H5a1 1 0 01-1-1v-6zM16 13a1 1 0 011-1h2a1 1 0 011 1v6a1 1 0 01-1 1h-2a1 1 0 01-1-1v-6z"/></svg>`,
        color: "violet",
        title: "UI/UX & Product Design",
        desc: "User-centric wireframing, high-fidelity UI design, and interactive prototyping. We turn complex business logic into intuitive, pleasant digital experiences.",
        features: [
          "User Research & Personas",
          "Wireframing & Workflows",
          "High-Fidelity UI Design",
          "Interactive Prototypes",
          "Design Systems",
          "Usability Testing",
        ],
      },
    ],
  },
  {
    categoryId: "marketing",
    categoryTitle: "Marketing & Growth",
    categoryDesc:
      "Data-backed search engine optimization, content strategy, and digital campaigns that drive qualified traffic and revenue.",
    color: "amber",
    items: [
      {
        id: "marketing",
        anchorIds: ["seo", "digital-marketing", "growth"],
        image: "/services/digital-marketing-seo.webp",
        imageAlt:
          "Digital marketing strategies, SEO performance and growth analytics",
        icon: `<svg width="32" height="32" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.8"><path stroke-linecap="round" stroke-linejoin="round" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"/></svg>`,
        color: "amber",
        title: "Digital Marketing & SEO",
        desc: "Targeted digital marketing, Search Engine Optimization (SEO), performance ad management, and conversion rate optimization engineered for high ROI.",
        features: [
          "Search Engine Optimization (SEO)",
          "Digital Marketing Strategy",
          "Social Media Campaigns",
          "Content & Growth Marketing",
          "Conversion Optimization",
          "Performance Analytics",
        ],
      },
    ],
  },
];
const faqs = [
  {
    question:
      "Do you only provide services, or do you build your own products?",
    answer:
      "We are a hybrid company. While we build custom software and SaaS platforms for our clients, we also engineer and operate our own proprietary SaaS products like N3 ServiceBook and N3 FitBook. This dual approach ensures we stay sharp on production-level operations, security, and scalability.",
  },
  {
    question: "What is your primary technology stack?",
    answer:
      "We are framework-agnostic but heavily favor modern, battle-tested technologies. For the frontend, we use React, Next.js, and Astro. For backend APIs, we rely on Node.js and scalable databases like PostgreSQL and MongoDB. For mobile, we build native-feeling apps using React Native and Flutter.",
  },
  {
    question: "Do you work with startups or established enterprises?",
    answer:
      "Both. We help founders build scalable MVPs from scratch, and we assist established enterprises in modernizing legacy systems, migrating to the cloud, and implementing digital transformation strategies.",
  },
  {
    question: "How do you handle project pricing and scoping?",
    answer:
      "We offer multiple engagement models. For well-defined scopes, we provide fixed-price contracts with clear milestones. For ongoing development or scaling SaaS platforms, we offer dedicated team retainers. We usually start with a paid discovery phase to map out the exact requirements and select the best model.",
  },
];
---

<Layout
  title="Software Development Services | Web, Mobile, Cloud & AI"
  description="End-to-end software development services from a Kerala-based team serving clients globally. Custom software, SaaS, web apps, mobile apps, cloud, AI/ML and digital transformation."
  keywords="software development services, custom software development, SaaS development, web development services, mobile app development services, cloud services, AI development services, software development Kerala, software company India"
  faqs={faqs}
>
  <Navbar />

  <!-- Page Hero -->
  <section class="page-hero">
    <div class="page-hero__glow"></div>
    <div class="container page-hero__inner">
      <h1 class="page-hero__title">
        Technology Services <br class="hide-sm" />
        <span class="text-gradient">Built for Impact</span>
      </h1>
    </div>
    <div class="page-hero__bg-dots"></div>
  </section>

  <!-- Interactive What We Do Overview (Stacked Cards Deck) -->
  <WhatWeDoCards showTitle={false} />

  <!-- Services List -->
  <section class="section services-page" id="services-page">
    <svg
      class="svc-thread"
      viewBox="0 0 1200 3600"
      preserveAspectRatio="xMidYMid slice"
      aria-hidden="true"
      focusable="false"
    >
      <defs>
        <linearGradient
          id="svcThread"
          gradientUnits="userSpaceOnUse"
          x1="0"
          y1="0"
          x2="1200"
          y2="3600"
        >
          <stop offset="0%" stop-color="#38bdf8"></stop>
          <stop offset="45%" stop-color="#8b5cf6"></stop>
          <stop offset="100%" stop-color="#22d3ee"></stop>
        </linearGradient>
      </defs>
      <path
        id="svc-thread-path"
        d="M 880,-40 C 900,220 320,300 300,560 C 280,820 900,880 880,1140 C 860,1400 300,1460 300,1720 C 300,1980 900,2020 880,2280 C 870,2540 600,2700 420,3200"
        fill="none"
        stroke="url(#svcThread)"
        stroke-width="18"
        stroke-linecap="round"
        stroke-linejoin="round"></path>
    </svg>

    <div class="container">
      <div class="services-list-grouped">
        {
          serviceCategories.map((cat) => (
            <div class="svc-category-group" id={cat.categoryId}>
              <div class="svc-category-header">
                <h2 class="svc-category-title">{cat.categoryTitle}</h2>
                <p class="svc-category-desc">{cat.categoryDesc}</p>
              </div>

              <div class="services-list">
                {cat.items.map((service, i) => (
                  <div
                    class={`svc-block svc-block--${service.color} ${i % 2 === 1 ? "svc-block--reverse" : ""}`}
                    id={service.id}
                    data-svc-block
                    style="position: relative;"
                  >
                    {service.anchorIds?.map((anchor) => (
                      <span
                        id={anchor}
                        style="position: absolute; top: -120px; left: 0; pointer-events: none; visibility: hidden;"
                      />
                    ))}
                    <div class="svc-block__visual">
                      <div class="svc-block__media">
                        <img
                          src={service.image}
                          alt={service.imageAlt}
                          width="900"
                          height="640"
                          loading="lazy"
                          decoding="async"
                        />
                      </div>
                      <div class="svc-block__features">
                        {service.features.map((f) => (
                          <div class="svc-feature">
                            <svg
                              width="14"
                              height="14"
                              viewBox="0 0 24 24"
                              fill="none"
                              stroke="var(--primary)"
                              stroke-width="3"
                            >
                              <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                d="M5 13l4 4L19 7"
                              />
                            </svg>
                            {f}
                          </div>
                        ))}
                      </div>
                    </div>
                    <div class="svc-block__content">
                      <h3 class="svc-block__title">{service.title}</h3>
                      <p class="svc-block__desc">{service.desc}</p>
                    </div>
                  </div>
                ))}
              </div>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Tech Stack -->
  <section class="section-sm tech-stack-section">
    <div class="container">
      <div class="section-header">
        <div class="badge badge-sky">Our Stack</div>
        <h2>The <span class="text-gradient">Tools</span> We Build With</h2>
        <p>
          We pick the right tool for the problem, not the other way around.
          Here's the stack we use most often across web, mobile, cloud, and SaaS
          projects.
        </p>
      </div>

      <div class="tech-grid">
        {
          [
            ["React", "Frontend"],
            ["Next.js", "Full-stack web"],
            ["Astro", "Marketing & docs"],
            ["Node.js", "Backend / APIs"],
            ["React Native", "Mobile cross-platform"],
            ["Flutter", "Mobile native UI"],
            ["PostgreSQL", "Relational database"],
            ["MongoDB", "Document database"],
            ["Firebase", "Realtime / auth"],
            ["AWS", "Cloud infrastructure"],
            ["Docker", "Containers"],
            ["Kubernetes", "Orchestration"],
          ].map(([name, role]) => (
            <div class="tech-tile">
              <span class="tech-tile__name">{name}</span>
              <span class="tech-tile__role">{role}</span>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Company Overview Banner -->
  <section class="company-overview-section">
    <div class="container">
      <p class="company-overview__desc">
        We are a hybrid <strong>product + service</strong> company building <a
          href="/custom-software-development/">custom software</a
        >, <a href="/saas-development-company/">SaaS platforms</a>,<br
          class="hide-sm"
        />
        web applications, and mobile apps for businesses in Kerala, India, and globally.<br
          class="hide-sm"
        />
        Every solution we ship is scalable, performance-focused, responsive, and user-centric.
      </p>
    </div>
  </section>

  <!-- Unified FAQ Section -->
  <FaqSection limit={6} />

  <!-- CTA -->
  <section class="section-sm">
    <div class="container">
      <div class="services-cta-box">
        <div class="services-cta-box__content">
          <h2>Ready to Transform Your Business?</h2>
          <p>
            Let's discuss exactly what you need and build a plan that works for
            your goals and budget.
          </p>
        </div>
        <div class="services-cta-box__actions">
          <a href="/contact" class="btn btn-primary btn-lg"
            >Start a Conversation</a
          >
          <a href="/work" class="btn btn-ghost btn-lg">See Our Work</a>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<style>
  /* Page hero */
  .page-hero {
    padding: calc(72px + 2.5rem) 0 1rem;
    background: var(--white);
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .page-hero__bg-dots {
    position: absolute;
    inset: 0;
    background-image: radial-gradient(
      circle,
      var(--sky-200) 1px,
      transparent 1px
    );
    background-size: 36px 36px;
    opacity: 0.25;
    pointer-events: none;
  }

  .page-hero__glow {
    position: absolute;
    width: 800px;
    height: 400px;
    background: radial-gradient(
      ellipse,
      rgba(14, 165, 233, 0.12) 0%,
      transparent 70%
    );
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    pointer-events: none;
  }

  .page-hero__inner {
    position: relative;
    z-index: 2;
  }

  .page-hero__inner .badge {
    margin-bottom: 1.25rem;
  }

  /* Same spec as .hero__title / .flow__title on the landing page, so the display
     voice is consistent across the site. */
  .page-hero__title {
    font-family: var(--font-sans);
    font-size: var(--display-lg);
    font-weight: 500;
    line-height: 0.95;
    letter-spacing: -0.04em;
    color: #000000;
    margin-bottom: 1.5rem;
  }

  .category-nav {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 1.5rem;
  }

  .cat-pill {
    padding: 0.5rem 1.25rem;
    border-radius: 50px;
    font-family: var(--font-sans);
    font-size: var(--body-sm);
    font-weight: 500;
    text-decoration: none;
    transition: all 0.25s ease;
    border: 1px solid var(--gray-200);
    background: #ffffff;
    color: var(--gray-700);
  }
  .cat-pill:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 16px rgba(0, 0, 0, 0.06);
  }
  .cat-pill--sky:hover {
    border-color: var(--sky-400);
    color: var(--sky-600);
  }
  .cat-pill--violet:hover {
    border-color: #a78bfa;
    color: #7c3aed;
  }
  .cat-pill--teal:hover {
    border-color: #5eead4;
    color: #0d9488;
  }
  .cat-pill--amber:hover {
    border-color: #fcd34d;
    color: #d97706;
  }

  .services-list-grouped {
    display: flex;
    flex-direction: column;
    gap: 6rem;
  }

  .svc-category-group {
    scroll-margin-top: 100px;
  }

  .svc-category-header {
    margin-bottom: 2.5rem;
    text-align: left;
    max-width: 720px;
  }

  .svc-category-title {
    font-family: var(--font-sans);
    font-size: var(--heading-md);
    font-weight: 500;
    letter-spacing: -0.03em;
    color: var(--gray-900);
    margin: 0.75rem 0 0.5rem 0;
  }

  .svc-category-desc {
    font-family: var(--font-sans);
    font-size: var(--body-md);
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .company-overview-section {
    padding: 3.5rem 0;
    background: #ffffff;
    text-align: center;
    border-top: 1px solid #e2e8f0;
    border-bottom: 1px solid #e2e8f0;
  }

  .company-overview__desc {
    font-family: var(--font-sans);
    color: var(--text-secondary);
    font-size: var(--body-lg);
    line-height: 1.7;
    max-width: 840px;
    margin: 0 auto;
    text-align: center;
  }

  .company-overview__desc a {
    color: var(--sky-700);
    text-decoration: underline;
    text-underline-offset: 3px;
  }
  .page-hero__desc a {
    color: var(--sky-700);
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .hide-sm {
    display: block;
  }

  /* Services blocks */
  .services-page {
    position: relative;
    overflow: hidden;
    isolation: isolate;
    padding-top: 1.5rem !important;
  }

  .svc-thread {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    opacity: 0.16;
    pointer-events: none;
  }

  .services-list {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    gap: 5rem;
  }

  /* --- entrance: halves slide in from opposite sides and settle with a bounce.
     The overshoot curve is what reads as "bounce" — a spring without a library. */
  .svc-block__visual,
  .svc-block__content {
    opacity: 0;
    transition:
      opacity 0.5s ease,
      transform 0.85s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  /* Default order: visual on the left, content on the right. */
  .svc-block__visual {
    transform: translate3d(-70px, 0, 0);
  }
  .svc-block__content {
    transform: translate3d(70px, 0, 0);
  }

  /* Reversed rows come from the other side, so the motion mirrors the layout. */
  .svc-block--reverse .svc-block__visual {
    transform: translate3d(70px, 0, 0);
  }
  .svc-block--reverse .svc-block__content {
    transform: translate3d(-70px, 0, 0);
  }

  .svc-block.is-visible .svc-block__visual,
  .svc-block.is-visible .svc-block__content {
    opacity: 1;
    transform: translate3d(0, 0, 0);
  }
  /* Text lands a beat after the image rather than with it. */
  .svc-block.is-visible .svc-block__content {
    transition-delay: 0.12s;
  }

  /* Everything resolved, nothing moving. Also drops the thread, which is purely
     decorative and only reads once it has drawn. */
  @media (prefers-reduced-motion: reduce) {
    .svc-block__visual,
    .svc-block__content,
    .svc-block.is-visible .svc-block__visual,
    .svc-block.is-visible .svc-block__content {
      opacity: 1;
      transform: none;
      transition: none;
    }
    .svc-block:hover .svc-block__media img {
      transform: none;
    }
    .svc-thread {
      display: none;
    }
  }

  .svc-block {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    scroll-margin-top: 100px;
  }

  .svc-block--reverse {
    direction: rtl;
  }

  .svc-block--reverse > * {
    direction: ltr;
  }

  /* Visual side */
  .svc-block__visual {
    background: var(--gray-50);
    border-radius: var(--radius-xl);
    border: 1.5px solid var(--gray-100);
    padding: 2.5rem;
    min-height: 320px;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  /* Placeholder imagery — swap the `image` field in the services array when the
     real assets land; nothing else needs to change. */
  .svc-block__media {
    position: relative;
    border-radius: var(--radius-lg);
    overflow: hidden;
    aspect-ratio: 3 / 2;
    background: var(--gray-100);
    box-shadow: 0 18px 40px -18px rgba(15, 23, 42, 0.35);
  }

  .svc-block__media img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.7s cubic-bezier(0.22, 1, 0.36, 1);
  }
  .svc-block:hover .svc-block__media img {
    transform: scale(1.04);
  }

  /* The old icon survives as a floating badge over the image. */
  .svc-block__icon-badge {
    position: absolute;
    left: 1rem;
    bottom: 1rem;
    width: 56px;
    height: 56px;
    border-radius: var(--radius-md);
    display: flex;
    align-items: center;
    justify-content: center;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    box-shadow: 0 6px 18px rgba(15, 23, 42, 0.18);
  }
  .svc-block__icon-badge svg {
    width: 26px;
    height: 26px;
  }

  .svc-block__icon-badge--sky {
    background: rgba(240, 249, 255, 0.92);
    color: var(--sky-600);
  }
  .svc-block__icon-badge--violet {
    background: rgba(245, 243, 255, 0.92);
    color: #7c3aed;
  }
  .svc-block__icon-badge--teal {
    background: rgba(240, 253, 250, 0.92);
    color: #0d9488;
  }
  .svc-block__icon-badge--amber {
    background: rgba(255, 251, 235, 0.92);
    color: #d97706;
  }
  .svc-block__icon-badge--rose {
    background: rgba(255, 241, 242, 0.92);
    color: #e11d48;
  }
  .svc-block__icon-wrap--indigo {
    background: #eef2ff;
    color: #4f46e5;
  }

  .svc-block__features {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.75rem;
  }

  .svc-feature {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-sans);
    font-size: var(--body-sm);
    color: var(--text-secondary);
    font-weight: 500;
  }
  .svc-feature svg {
    flex-shrink: 0;
  }

  /* Content side */
  .svc-block__content {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: flex-start;
  }

  .svc-block__content .badge {
    width: auto;
  }

  .svc-block__title {
    font-family: var(--font-sans);
    font-size: var(--heading-sm);
    font-weight: 500;
    letter-spacing: -0.02em;
    color: var(--gray-900);
  }

  .svc-block__desc {
    font-family: var(--font-sans);
    color: var(--text-secondary);
    line-height: 1.7;
    font-size: var(--body-md);
    margin-bottom: 0.5rem;
  }

  /* Badge colors */
  .badge-sky {
    background: var(--sky-50);
    color: var(--sky-700);
    border: 1px solid var(--sky-100);
  }
  .badge-violet {
    background: #f5f3ff;
    color: #7c3aed;
    border: 1px solid #ddd6fe;
  }
  .badge-teal {
    background: #f0fdfa;
    color: #0d9488;
    border: 1px solid #ccfbf1;
  }
  .badge-amber {
    background: #fffbeb;
    color: #d97706;
    border: 1px solid #fde68a;
  }
  .badge-rose {
    background: #fff1f2;
    color: #e11d48;
    border: 1px solid #fecdd3;
  }
  .badge-indigo {
    background: #eef2ff;
    color: #4f46e5;
    border: 1px solid #c7d2fe;
  }

  /* Tech stack */
  .tech-stack-section {
    background: linear-gradient(180deg, #ffffff 0%, #f8fafc 100%);
    padding-top: 4rem;
    padding-bottom: 4rem;
  }
  .tech-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
    max-width: 980px;
    margin: 0 auto;
  }
  .tech-tile {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
    padding: 1.1rem 1.25rem;
    background: #ffffff;
    border: 1.5px solid var(--gray-100);
    border-radius: var(--radius-md);
    transition: var(--transition);
  }
  .tech-tile:hover {
    transform: translateY(-2px);
    border-color: var(--sky-200);
    box-shadow: 0 6px 16px rgba(14, 165, 233, 0.08);
  }
  .tech-tile__name {
    font-family: var(--font-sans);
    font-size: 1rem;
    font-weight: 500;
    color: var(--gray-900);
    letter-spacing: -0.01em;
  }
  .tech-tile__role {
    font-family: var(--font-sans);
    font-size: var(--body-sm);
    color: var(--text-muted);
    font-weight: 400;
  }
  @media (max-width: 800px) {
    .tech-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (max-width: 480px) {
    .tech-grid {
      grid-template-columns: 1fr;
    }
  }

  /* CTA box */
  .services-cta-box {
    background: linear-gradient(135deg, var(--sky-50) 0%, var(--white) 100%);
    border: 1.5px solid var(--sky-100);
    border-radius: var(--radius-xl);
    padding: 3.5rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
  }

  .services-cta-box__content h2 {
    font-family: var(--font-sans);
    font-size: var(--heading-sm);
    font-weight: 500;
    letter-spacing: -0.02em;
    margin-bottom: 0.75rem;
  }

  .services-cta-box__content p {
    font-family: var(--font-sans);
    color: var(--text-secondary);
    font-size: var(--body-md);
    line-height: 1.6;
  }

  .services-cta-box__actions {
    display: flex;
    gap: 1rem;
    flex-shrink: 0;
    flex-wrap: wrap;
  }

  @media (max-width: 900px) {
    .svc-block,
    .svc-block--reverse {
      grid-template-columns: 1fr;
      direction: ltr;
      gap: 2rem;
    }
    .services-cta-box {
      flex-direction: column;
      text-align: center;
    }
    .services-cta-box__actions {
      justify-content: center;
    }
  }

  @media (max-width: 640px) {
    .hide-sm {
      display: none;
    }
    .svc-block__visual {
      padding: 1.25rem;
      gap: 1.25rem;
      min-height: auto;
    }
    .svc-block__features {
      grid-template-columns: repeat(2, 1fr);
      gap: 0.6rem 0.5rem;
    }
    .svc-feature {
      font-size: 0.8rem;
      gap: 0.35rem;
    }
  }

  /* ===== STACKED CARDS SCROLL SECTION HEADER & LAYOUT ===== */
  .stacked-section-header {
    display: flex !important;
    justify-content: center !important;
    align-items: center !important;
    text-align: center !important;
    margin: 0 auto 3.2rem auto !important;
    width: 100% !important;
  }

  .stacked-section-title {
    font-family: var(--font-sans) !important;
    font-size: var(--heading-md) !important;
    font-weight: 500 !important;
    color: #0f172a !important;
    line-height: 1.1 !important;
    letter-spacing: -0.02em !important;
    margin: 0 auto !important;
    text-align: center !important;
    display: block !important;
    width: 100% !important;
  }


</style>

<script is:inline>
  (function () {
    var reduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

    // --- blocks slide in from their own side, once each
    var blocks = document.querySelectorAll("[data-svc-block]");
    if (reduced) {
      blocks.forEach(function (b) {
        b.classList.add("is-visible");
      });
    } else if (blocks.length) {
      var blockIO = new IntersectionObserver(
        function (entries) {
          entries.forEach(function (e) {
            if (!e.isIntersecting) return;
            e.target.classList.add("is-visible");
            blockIO.unobserve(e.target); // one-shot
          });
        },
        { threshold: 0.2, rootMargin: "0px 0px -8% 0px" },
      );
      blocks.forEach(function (b) {
        blockIO.observe(b);
      });
    }

    // --- thread draws itself as the section scrolls past
    var section = document.getElementById("services-page");
    var path = document.getElementById("svc-thread-path");
    if (reduced || !section || !path) return;

    var len = path.getTotalLength();
    path.style.strokeDasharray = String(len);
    path.style.strokeDashoffset = String(len);

    var inView = false;
    var raf = null;

    function wake() {
      if (inView && raf === null) raf = requestAnimationFrame(draw);
    }

    function draw() {
      raf = null;
      var r = section.getBoundingClientRect();
      var vh = window.innerHeight;
      // 0 -> 1 across the whole section travelling through the viewport
      var p = Math.max(0, Math.min(1, (vh - r.top) / (vh + r.height)));
      path.style.strokeDashoffset = String(len * (1 - p));
    }

    new IntersectionObserver(
      function (entries) {
        inView = entries[0].isIntersecting;
        wake();
      },
      { rootMargin: "200px 0px" },
    ).observe(section);

    // Purely scroll-driven, so one pass per scroll event is enough — no loop.
    window.addEventListener("scroll", wake, { passive: true });
    window.addEventListener("resize", wake, { passive: true });
    draw();
  })();
</script>
